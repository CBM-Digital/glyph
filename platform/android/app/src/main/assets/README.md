Glyph game assets are copied into the APK at build time.

The Gradle `syncGlyphArcadeAssets` task packages the repository sample at
`examples/arcade` as `examples/arcade` inside the Android assets directory.
