Placeholder asset paths for the headless milestone 5 runtime.

The current backend-independent runtime validates manifest entries by extension and resolves asset handles.
It does not decode image, font, or audio files yet.
